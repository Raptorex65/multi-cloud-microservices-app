version: "3.9"

services:
  frontend:
    build:
      context: ./frontend
    ports:
      - "8081:8080"

  api:
    build:
      context: ./api
    ports:
      - "8080:8080"

  worker:
    build:
      context: ./worker